## 🚨 Canvas Core 개발자 필수 준수 사항 🚨

이 문서는 "Canvas Core" 프로젝트의 품질과 일관성을 유지하기 위한 핵심 규약입니다. 모든 개발자는 작업을 시작하기 전 이 문서를 숙지하고 **반드시** 준수해야 합니다.

### 1. 사전 확인 의무

* **작업을 수행하기 전, 반드시 해당 작업과 관련된 패키지 폴더 내에 `CLAUDE.md` 파일이 있는지 확인하고, 존재한다면 그 내용을 먼저 읽고 작업을 시작해야 합니다.**

### 2. 최신 정보 확인

* **기술적으로 모호한 부분이 발생하면, 추측에 의존하지 말고 반드시 2025년 기준 최신 웹 검색 결과를 참조하여 가장 현대적인 방식으로 문제를 해결해야 합니다.**

### 3. 코드 작성 규칙

* **논리적 null 병합 연산은 `||` 대신 `??` (Nullish Coalescing Operator)를 사용해야 합니다.**
* **TypeScript 파일(`import` 구문)에서 다른 TypeScript 파일을 불러올 때, 모듈 경로 끝에 `.js`가 아닌 원본 확장자인 `.ts`를 명시해야 합니다.** (예: `import { Scene } from './Scene.ts';`)

### 4. 네이밍 컨벤션

* **약어 사용을 엄격히 금지합니다. 항상 완전한 단어(full word)를 사용해야 합니다.**
    * (X) `dist` -> (O) `outputs`
    * (X) `src` -> (O) `sources`
    * (X) `test` -> (O) `tests`

* **예외: 널리 통용되는 특정 약어(`ID`, `URL`, `UUID`, `JSON`)는 예외적으로 허용합니다.**

* **허용된 약어(`ID`, `URL` 등)가 접두사로 사용되는 경우를 제외하고, 단독으로 사용될 때는 항상 전체를 대문자로 표기해야 합니다.**
    * (X) `userID` (접두사가 아님)
    * (O) `userId`
    * (O) `userID` (단독 사용 예시 - 변수명이 `userID`인 경우)
    * (O) `parseJSON` (접두사로 사용된 예시)
    * (O) `IdGenerator` (X) -> `IDGenerator` (O)
