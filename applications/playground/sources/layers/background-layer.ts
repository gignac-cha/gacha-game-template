import { Layer } from '@gacha-game-template/layer';

/**
 * BackgroundLayer - Renders the background of a scene
 */
export class BackgroundLayer extends Layer {
  constructor(element: HTMLCanvasElement) {
    super(element);
  }
}
