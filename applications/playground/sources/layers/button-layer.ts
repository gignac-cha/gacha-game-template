import { Layer } from '@gacha-game-template/layer';

/**
 * ButtonLayer - Renders interactive buttons
 */
export class ButtonLayer extends Layer {
  constructor(element: HTMLCanvasElement) {
    super(element);
  }
}
