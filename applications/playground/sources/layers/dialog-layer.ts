import { Layer } from '@gacha-game-template/layer';

/**
 * DialogLayer - Renders dialog boxes and modals
 */
export class DialogLayer extends Layer {
  constructor(element: HTMLCanvasElement) {
    super(element);
  }
}
