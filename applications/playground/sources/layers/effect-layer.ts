import { Layer } from '@gacha-game-template/layer';

/**
 * EffectLayer - Renders visual effects and animations
 */
export class EffectLayer extends Layer {
  constructor(element: HTMLCanvasElement) {
    super(element);
  }
}
