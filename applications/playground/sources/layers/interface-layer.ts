import { Layer } from '@gacha-game-template/layer';

/**
 * InterfaceLayer - Renders UI elements and interface components
 */
export class InterfaceLayer extends Layer {
  constructor(element: HTMLCanvasElement) {
    super(element);
  }
}
