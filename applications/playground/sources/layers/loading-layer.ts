import { Layer } from '@gacha-game-template/layer';

/**
 * LoadingLayer - Renders loading indicators and text
 */
export class LoadingLayer extends Layer {
  constructor(element: HTMLCanvasElement) {
    super(element);
  }
}
