import { Layer } from '@gacha-game-template/layer';

/**
 * NotificationLayer - Renders notifications and alerts
 */
export class NotificationLayer extends Layer {
  constructor(element: HTMLCanvasElement) {
    super(element);
  }
}
